@model CategoryViewModel

@{
    ViewData["Title"] = "Delete Category"; // More informative title
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    /* Styling for Delete Category Confirmation Page - Reusing Delete News Article Styles */

    :root {
        --primary-color: #0056b3;
        --secondary-color: #6c757d;
        --background-color: #f8f9fa;
        --text-color: #343a40;
        --accent-color: #ffc107;
        --delete-card-background: #ffffff;
        --delete-card-border: #dee2e6;
        --delete-card-border-radius: 8px;
        --delete-warning-color: #dc3545; /* Danger/Warning color for delete messages */
        --meta-color: var(--secondary-color);
    }

    body {
        background-color: var(--background-color);
        color: var(--text-color);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .delete-container {
        max-width: 800px; /* Adjust as needed */
        margin: 30px auto; /* More top margin to stand out */
        padding: 30px;
        background-color: var(--delete-card-background);
        border: 1px solid var(--delete-card-border);
        border-radius: var(--delete-card-border-radius);
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.1); /* Slightly stronger shadow */
        text-align: center; /* Center content within the container */
    }

    h1 {
        color: var(--delete-warning-color); /* Red title to highlight delete action */
        margin-bottom: 20px;
        font-size: 2.5em;
    }

    h3 {
        color: var(--text-color);
        margin-bottom: 20px;
        font-size: 1.4em;
        font-weight: normal; /* Keep it less bold than title */
    }

    .category-details {
        text-align: left; /* Align category details to the left within the container */
        margin-bottom: 30px;
    }

    .detail-row {
        display: flex;
        margin-bottom: 10px;
    }

    .detail-label {
        flex-basis: 25%; /* Adjust as needed for label width */
        font-weight: bold;
        color: var(--meta-color);
        padding-right: 10px;
        text-align: right; /* Align labels to the right */
    }

    .detail-value {
        flex-basis: 75%; /* Adjust as needed for value width */
        color: var(--text-color);
    }

    .delete-actions {
        margin-top: 30px;
        text-align: center; /* Center buttons */
    }

    .btn-danger {
        padding: 0.9rem 2rem; /* Larger delete button */
        font-size: 1.1rem;
        border-radius: 0.3rem;
        font-weight: bold;
    }

    .btn-secondary {
        padding: 0.8rem 1.8rem;
        font-size: 1rem;
        border-radius: 0.3rem;
    }

</style>

<div class="delete-container">
    <h1><i class="fas fa-exclamation-triangle" style="color: var(--delete-warning-color);"></i> Delete Category?</h1>

    <h3>Are you absolutely sure you want to delete this category? This action cannot be undone.</h3>

    <div class="category-details">
        <div class="detail-row">
            <div class="detail-label">@Html.DisplayNameFor(model => model.CategoryName)</div>
            <div class="detail-value">@Html.DisplayFor(model => model.CategoryName)</div>
        </div>
        <div class="detail-row">
            <div class="detail-label">@Html.DisplayNameFor(model => model.CategoryDescription)</div>
            <div class="detail-value">@Html.DisplayFor(model => model.CategoryDescription)</div>
        </div>
        <div class="detail-row">
            <div class="detail-label">@Html.DisplayNameFor(model => model.IsActive)</div>
            <div class="detail-value">@Html.DisplayFor(model => model.IsActive)</div>
        </div>
        <div class="detail-row">
            <div class="detail-label">@Html.DisplayNameFor(model => model.ParentCategory)</div>
            <div class="detail-value">
                @Html.DisplayFor(model => model.ParentCategory.CategoryDescription)
            </div>
        </div>
    </div>

    <div class="delete-actions">
        <form asp-action="Delete">
            <input type="hidden" asp-for="CategoryId" />
            <input type="submit" value="Confirm Delete" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancel - Back to List</a>
        </form>
    </div>
</div>